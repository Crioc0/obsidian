---
tags:
  - база
created: 2026-01-24
related:
  - "[[Объекты в JS]]"
repeat: spaced every 108 hours
due_at: 2026-06-29T09:13:42.591+03:00
---

# Прототипы объектов

Объекты в JS имеют **прототипное наследование**. При обращении к свойству:

1. Сначала ищется своё свойство.
2. Если не найдено — поиск идёт по цепочке прототипов.

Это позволяет экономить память (методы не копируются в каждый объект), но добавляет сложность при проверке наличия ключа (`hasOwnProperty`).

```js
const parent = {inherited: "value"};
const child = Object.create(parent);
child.own = "own value";

console.log(child.inherited); // "value" — из прототипа
console.log(child.own); // "own value"
console.log(child.toString); // из прототипа Object
```
## Ссылки

- 