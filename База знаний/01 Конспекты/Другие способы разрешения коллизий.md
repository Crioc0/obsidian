---
tags:
  - база
created: 2026-01-24
related:
  - "[[Коллизии хэшей]]"
repeat: spaced every 72 hours
due_at: 2026-06-24T12:04:32.250+03:00
---

# Другие способы разрешения коллизий
### Открытая адресация (open addressing)

При коллизии все элементы хранятся прямо в массиве, без внешних списков. Ищется другая свободная ячейка по определённому правилу.

### Линейное пробирование (linear probing)

При коллизии проверяем следующую ячейку: `index = (index + 1) % size`. Повторяем, пока не найдём пустую.

**Проблема:** образуются кластеры (группы занятых ячеек), что замедляет поиск.

### Квадратичное пробирование (quadratic probing)

Шаг увеличивается квадратично: `index = (index + k²) % size`. Это уменьшает образование кластеров.

### Двойное хэширование (double hashing)

Используется вторая хэш-функция: `index = (index + step) % size`, где `step = secondHash(key)`. Это один из лучших методов открытой адресации.

## Проблемы открытой адресации


- Требует тщательного удаления элементов (нельзя просто очистить ячейку — нужно маркировать как «удалено» или перестраивать кластер).
- Коэффициент заполнения должен быть низким (обычно < 0.5–0.6), иначе производительность резко падает.
- При удалениях образуются «дырки», ухудшающие поиск.

Метод цепочек с помощью списков или массивов проще и надёжнее для большинства задач.
## Ссылки

- 