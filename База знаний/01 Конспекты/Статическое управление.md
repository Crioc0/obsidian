---
tags:
  - база
created: 2026-01-24
related:
  - "[[Управление памятью]]"
repeat: spaced every 349 hours
due_at: 2026-06-30T06:00:00.000+03:00
---

# Статическое управление
Под «статической моделью» в контексте современных языков чаще всего понимают подход Rust, а также связанные с ним идеи ( например, циклический анализатор ссылок в Swift). Ключевая идея: **компилятор статически (на этапе компиляции) доказывает корректность управления памятью**, без использования сборщика мусора и без необходимости вручную вызывать `malloc`/`free`.

**Rust и его модель владения:**

- У каждого значения есть **единственный владелец** (owner).
- Когда владелец выходит из области видимости, память автоматически освобождается (вызывается деструктор, аналог RAII в C++).
- Передача значения другому владельцу (перемещение) запрещает доступ из исходного места.
- **Заимствование (borrowing)** — ссылки (`&T` и `&mut T`) с проверкой времён жизни (lifetimes) компилятором.
- Компилятор проверяет, что ссылки не переживают данные, на которые указывают (нет висячих ссылок).

```rust
fn example() {
let s = String::from("hello"); // выделена память в куче
let t = s;                     // владение перемещено в t, s больше недействительна
// println!("{}", s);          // ошибка компиляции!
println!("{}", t);             // OK
} // здесь t выходит из области видимости, память освобождена
```

**Что можно размещать:**

- **На стеке** — значения с известным на этапе компиляции размером (простые структуры, кортежи, массивы фиксированной длины, ссылки).
- **В куче** — динамические структуры (`Box<T>`, `Vec<T>`, `String`, `Rc<T>`, `Arc<T>`), но компилятор отслеживает время жизни и освобождение.

**Сравнение с другими подходами:**

|Подход|Освобождение|Проверка|Примеры языков|
|---|---|---|---|
|Ручное|`free`/`delete`|Ручная (или анализаторы)|C, C++|
|RAII|деструктор|Ручная (язык гарантирует вызов, но не защиту от висячих ссылок)|C++|
|Статическая (Rust)|деструктор + проверка владения|Компилятор (строгая, гарантированная)|Rust|
|Счётчики ссылок|немедленно при падении до нуля|Рантайм|Swift (ARC), Python (+ GC)|
|Сборщик мусора|периодически|Рантайм|Java, C#, Go, JS|

**Преимущества статической модели Rust:**

- **Безопасность памяти** — никаких утечек, двойных освобождений, use-after-free, гонок данных по памяти (data races) на этапе компиляции.
- **Производительность** — никакого сборщика мусора (нет пауз), никакого оверхеда на счётчики ссылок (кроме явных `Rc`/ `Arc`).
- **Предсказуемость** — освобождение происходит в известные моменты (конец области видимости).

**Ограничения:**

- Некоторые паттерны (циклические структуры, графы) трудно выразить без `Rc<RefCell<T>>` или `Weak`, что возвращает часть оверхеда.
- Кривая обучения — проверка заимствований и времён жизни требует привыкания.

**Другие языки с элементами статического управления:**

- **C++** — умные указатели (`unique_ptr`, `shared_ptr`) приближаются к модели Rust, но проверки только рантаймовые ( кроме `unique_ptr` на уровне семантики перемещения, но без гарантий компилятора).
- **Swift** — ARC (автоматический подсчёт ссылок) — это рантайм, но с сильной статической оптимизацией.
- **Zig** — ручное управление, но с `defer` и аллокаторами как параметрами, без статической проверки.
## Ссылки

- 