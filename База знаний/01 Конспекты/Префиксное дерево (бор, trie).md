---
tags:
  - алгоритмыПоискаПоСтроке
created: 2026-01-24
related:
  - "[[Алгоритмы поиска по строке]]"
repeat: spaced every 401 hours
due_at: 2026-09-16T06:00:00.000+03:00
---

# Префиксное дерево (бор, trie)

**Бор (trie)** — дерево, в котором:

- Корень — пустая строка.
- Каждое ребро помечено символом.
- Путь от корня до узла образует строку (префикс).
- Узлы могут быть терминальными (отмечают конец слова).
![[Префиксное дерево.png]]
**Операции:**

- Вставка — O(L), где L — длина слова.
- Поиск — O(L).
- Поиск всех слов с префиксом — O(P + количество результатов), где P — длина префикса.

### Реализация бора

Узел бора содержит:

- Массив или хэш-таблицу детей (символ → узел).
- Флаг терминальности.
- Дополнительные данные (например, индекс паттерна).

**Вставка:**

1. Начинаем с корня.
2. Для каждого символа слова:
    - Если ребра нет — создаём новый узел.
    - Переходим по ребру.
3. Помечаем последний узел как терминальный.

**Поиск:**

1. Начинаем с корня.
2. Для каждого символа:
    - Если ребра нет — слова нет.
    - Переходим по ребру.
3. Если узел терминальный — слово найдено.

### Сжатый бор

В обычном боре много узлов с одним ребёнком. **Сжатый бор (radix tree, Patricia trie)** объединяет такие узлы в один, храня ребро как строку, а не символ.
![[Сжатый бор.png]]
**Преимущества:** экономия памяти, ускорение поиска. **Недостаток:** усложнение операций вставки и удаления.
## Ссылки

- 