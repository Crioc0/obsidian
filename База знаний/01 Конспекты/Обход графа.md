---
tags:
  - графы
created: 2026-01-24
related:
  - "[[Графы]]"
repeat: spaced every 81 hours
due_at: 2026-07-13T03:45:20.836+03:00
---

# Обход графа

Обход графа — это алгоритм, который посещает каждую вершину графа (или проходит по всем рёбрам) в определённом порядке. Два основных типа:

### Поиск в глубину (DFS — Depth-First Search)

Идём «вглубь»: от текущей вершины переходим к первому непосещённому соседу, затем от него — дальше. Если дальше идти некуда — возвращаемся (бэктрекинг).

![[Обход в глубину.png]]

**Реализация (рекурсивная):**

```js
function dfs(graph, vertex, visited = new Set()) {
    visited.add(vertex);
    console.log(vertex);

    for (const neighbor of graph[vertex]) {
        if (!visited.has(neighbor)) {
            dfs(graph, neighbor, visited);
        }
    }
}
```

**Реализация (итеративная с явным стеком):**

```js
function dfsIterative(graph, start) {
    const visited = new Set();
    const stack = [start];

    while (stack.length > 0) {
        const vertex = stack.pop();
        if (visited.has(vertex)) continue;
        visited.add(vertex);
        console.log(vertex);

        for (const neighbor of graph[vertex]) {
            if (!visited.has(neighbor)) {
                stack.push(neighbor);
            }
        }
    }
}
```

**Сложность:** O(V + E) при использовании списка смежности.

### Поиск в ширину (BFS — Breadth-First Search)


Идём «вширь»: сначала посещаем всех соседей текущей вершины, затем соседей соседей и так далее. Используется очередь ( FIFO).

![[Поиск в ширину.png]]

```js
function bfs(graph, start) {
    const visited = new Set();
    const queue = [start];

    while (queue.length > 0) {
        const vertex = queue.shift();
        if (visited.has(vertex)) continue;
        visited.add(vertex);
        console.log(vertex);

        for (const neighbor of graph[vertex]) {
            if (!visited.has(neighbor)) {
                queue.push(neighbor);
            }
        }
    }
}
```

**Сложность:** O(V + E).

## Сравнение DFS и BFS: стек → глубина, очередь → ширина


|Аспект|DFS|BFS|
|---|---|---|
|Структура данных|Стек (явный или рекурсия)|Очередь|
|Порядок обхода|Вглубь по одному пути|По слоям (вширь)|
|Память в худшем случае|O(V) — при глубоком графе|O(V) — при широком графе|
|Находит кратчайший путь|Нет (обходит всех)|Да (в невзвешенном графе)|
|Использует рекурсию|Естественно|Нет|

**Когда что использовать:**

- **DFS** — проверка существования пути, топологическая сортировка, поиск компонент связности, обход в глубину дерева.
- **BFS** — поиск кратчайшего пути в невзвешенном графе, проверка двудольности, обход по слоям.

## Ссылки

- 