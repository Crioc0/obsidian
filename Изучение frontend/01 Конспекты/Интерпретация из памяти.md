---
tags:
  - review
created: 2026-01-24
related:
  - "[[Бит и байт]]"
repeat: spaced every 24 hours
due_at: 2026-02-27T14:05:30.956+03:00
---
# Интерпретация из памяти
Если наше число не может иметь знак, то в 8-ми битах мы можем хранить значение от 0 до 255 (256 значение) А если может – от -128 до 127 (тоже 256 значений) Мы можем посмотреть на одни и те же биты как на число со знаком или без

```js
const a = new Int8Array([-125]); 
const b = new Uint8Array(a.buffer); 

console.log(a[0]); // -125 
console.log(b[0]); // 131
```

```js
// Отладчик JS выводит числа в 10-тиричной системе 
console.log(parseInt("10", 2)); // 2 
console.log(parseInt("10", 8)); // 8 
console.log(parseInt("10", 16)); // 16 
console.log(parseInt("10", 36)); // 36
```
ВИЗУАЛЬНО РАЗНЫЕ ЧИСЛА МОГУТ БЫТЬ ОДИНАКОВЫМИ
```js
console.log(0b1111_1111 === 255); // true 
console.log(0xFF === 255); // true 
console.log(0xFF === 0o377); // true
```

Система счисления позволяет записать и прочитать число Число «двести пятьдесят пять» может быть записано как: 255, FF или 1111 1111 Но оно все равно остается «двести пятьдесят пять»


![[Интерпретация бит из памяти.png]]
## Ссылки
