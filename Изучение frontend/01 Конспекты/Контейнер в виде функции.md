---
tags:
  - review
created: 2026-01-24
related:
repeat: spaced every 96 hours
due_at: 2026-02-06T16:18:39.524+03:00
---
# Контейнер в виде функции


## Что это? (Определение)
Функии - тоже контейнеры. Они - это абстракции над вычислениями.

Например,
```js
add2 = (x) => x + 2
```
 Аргумент x является контекстом, а вычисление – «эффектом» Чтобы извлечь значение из контекста – надо вызвать функцию Чтобы упаковать в контейнер – упаковать в функцию Например, 
 ```js
  cnst = (value) => (_) => value; cnst(42); // (_) => 42
 ```


## Зачем это нужно? (Применение)
Функции действительно можно рассматривать, как обычные контейнеры И наделить их стандартными интерфейсами функтора, аппликативного функтора и монады

## Как это работает? (Принцип)


## Пример 


## Ссылки