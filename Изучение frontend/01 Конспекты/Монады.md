---
tags:
  - концепция
  - review
created: 2026-01-24
related:
  - "[[Функциональное программирование]]"
repeat: daily
due_at: 2026-01-25T16:03:38.464+03:00
---
# Монады


## Что это? (Определение)
Если контейнер поддерживает интерфейс такого типа, то он называется монадой
```js
flatMap(fn) { return this.isNone() ? this : fn(this.#value); }
```

## Зачем это нужно? (Применение)


## Как это работает? (Принцип)
Разберем монаду на примере flatMap(fn)
Функция fn ожидает чистое значение, но возвращает контейнер.(тип возвращаемого контейнера совпадает с исходным контейнером). Результатом работы всей функции явя

## Пример 