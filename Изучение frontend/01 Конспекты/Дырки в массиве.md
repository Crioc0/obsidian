---
tags:
  - база
created: 2026-01-24
related:
  - "[[Массивы в JS]]"
repeat: spaced every 96 hours
due_at: 2026-04-17T09:18:25.915+03:00
---

# Дырки в массиве

Создание дырок в массиве вынуждает VM использовать другую структуру данных под капотом, что приводит к резкому падению производительности

```js
let a = []; 

// Убийца производительности 
a[100] = 42; 

// Сразу просим нужный размер и заполняем его значением по умолчанию 
let b = new Array(101).fill(0); 

// Теперь все хорошо 
b[100] = 42;

```

```js
const a = new Array(100); 
%DebugPrint(a); // HOLEY_SMI_ELEMENTS 

for (let i = 0; i < a.length; i++) { 
	a[i] = 0; 
} 
%DebugPrint(a); // Обратите внимание, все еще HOLEY_SMI_ELEMENTS 

const b = [0, 1, 2, 3, 4, 5]; 
%DebugPrint(b); // PACKED_SMI_ELEMENTS 

b[1000] = 42; 
%DebugPrint(b); // Обратите внимание, теперь HOLEY_SMI_ELEMENTS 

const с = new Array(100).fill(0); 
%DebugPrint(с); // PACKED_SMI_ELEMENTS 

const d = new Array(100).fill([]); 
%DebugPrint(d); // PACKED_ELEMENTS
```

В V8 у массивов есть свои «подтипы»: массивы с дырками (holey), упакованный (packed), а также по типам элементов. При этом тип определяется при создании массива и может измениться в «худшую» сторону (более абстрактный тип). А вот в «лучшую» не может (стать более конкретным). Иногда это может быть причиной падения производительности.

Предварительное задание длины массива снимает негативный эффект от дырок Если вы сами не инициализируете элементы такого массива, то используйте fill – VM скажет вам спасибо

## Ссылки

- 