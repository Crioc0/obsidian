---
tags:
  - база
created: 2026-01-24
related:
  - "[[Процессор и память]]"
repeat: spaced every 24 hours
due_at: 2026-04-05T15:11:48.461+03:00
---

# Кэш-память

Кэш память, или же кэш процессора является частью процессора, но медленней регистров. Может содержать куда больше информации, и все еще быстрее DRAM.

Данные из DRAM сперва попадают в кэш. Когда процессор хочет работать с данными из оперативной памяти, он предварительно проверяет, нет ли этих данных в кэше. Если их нет (кэш-промах), то будет честный поход в оперативную память. Загруженные данные сохраняются в кэше, а уже потом попадут на регистр. Когда за данными ходить не надо (кэш-попадание) - наша программа будет работать значительно быстрее.

Размер кэша значительно меньше возможного размера DRAM. Из-за этого приходится постоянно загружать и выгружать данные из кэша. Этой логикой занимается контроллер кэша. Помимо самих данных из DRAM, в кэше нужно хранить их адреса. Чтобы не хранить адрес каждого каждого байта, их объединяют в группы - кэш-линии. Тогда нужно будет хранить только адрес кэш-линии. Размер линии на x86 равен 64 байтам.

Если данные решат рядом в памяти, то они с высокой долей вероятности будут лежать рядом и в кэше. Когда мы обходим массив последовательно, получается минимизировать обращение к DRAM за счет использования кэша

![[Pasted image 20260406104125.png]] 

## Ссылки

- 